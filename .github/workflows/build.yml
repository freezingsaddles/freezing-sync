---
name: Build

on: pull_request  # yamllint disable-line

permissions:
  contents: read

jobs:
  build:
    permissions:
      contents: read
    uses: freezingsaddles/freezing-web/.github/workflows/build-docker.yml@188155b3d3466278116cafb2a3b3c1c2c4773613 # harden-workflows, pending 1.7.0
    with:
      tag: latest-actions-build
      repo: freezing-sync
      push: false
